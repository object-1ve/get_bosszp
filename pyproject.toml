[project]
name = "getdata"
version = "0.1.0"
description = "BOSS直聘数据采集 — 搜索职位、获取详情、存入数据库"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.60.0",
    "requests>=2.28",
]

[project.optional-dependencies]
playwright = [
    "playwright>=1.40",
]
